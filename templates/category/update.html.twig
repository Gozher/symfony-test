{% extends 'base.html.twig' %}

{% block title %}Actualizacion de categoria!!{% endblock %}

{% block body %}

<h3>Actualizacion de categorias</h3>
<div class="container">
  <div class="row justify-content-md-center">
    
    <div class="col col-lg-4 ">
          
      {{ form(form) }}
      <a href="{{ path('app_category') }}" class="btn btn-secondary">Regresar</a>
    </div>
    
  </div>
</div>

{% endblock %}
